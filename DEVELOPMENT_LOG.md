# 图片工具开发过程记录

> 开始时间：2026-07-16（Asia/Shanghai）  
> 记录规则：从本次开发开始，持续记录每次操作、目的、结果和下一步；涉及代码修改时列出主要文件，涉及验证时记录实际结果。

## 2026-07-16

### 01. 确认开发约定

- 操作：确认开始开发移动端优先的图片格式转换、压缩、体积调整、分辨率修改和模板裁剪网站。
- 目的：将已确认的浅色/深色移动端原型转化为可运行产品。
- 结果：采用现有 `PROJECT_ARCHITECTURE.md` 与 `prototypes/mobile-prototype-light-dark-v2.png` 作为产品和视觉基线。
- 约定：后续命令、修改、验证、问题和决策均追加到本文档。

### 02. 读取网站构建规范

- 操作：读取 `sites:sites-building` 技能说明。
- 目的：按当前 Sites 项目的标准结构完成初始化、开发、验证与后续上线。
- 结果：确认本项目属于带图片上传/处理能力的 capability path；首先初始化站点并启动本地预览，然后开发移动端产品界面与本地图片处理能力。

### 03. 初始化站点工程

- 操作：使用 Sites 官方初始化脚本在当前目录创建项目并安装基础依赖。
- 目的：获得标准的可开发、可构建和可部署站点结构。
- 结果：初始化器在写入前检测到当前目录非空并安全退出，未修改现有文件。
- 问题：架构文档、过程记录和原型图使初始化器无法直接创建项目。
- 恢复方案：将三项现有资料临时移动到 `/tmp/pic_tools_preinit`，在原目录重试初始化，完成后原样恢复。

### 04. 临时保护已有资料

- 操作：临时移动 `PROJECT_ARCHITECTURE.md`、`DEVELOPMENT_LOG.md` 和 `prototypes/`。
- 目的：在不丢失既有成果的前提下让初始化器使用空目录。
- 结果：已有资料已安全暂存；初始化脚本随后成功创建站点并安装 509 个基础依赖；资料已全部恢复到项目根目录。
- 说明：安装过程只有两个上游包的 deprecated 警告，没有安装失败。

### 05. 启动本地开发预览

- 操作：启动站点开发服务并保留进程。
- 目的：让后续界面修改可以即时预览，同时尽早确认项目可运行。
- 结果：首次启动被受限环境阻止监听调试端口 `0.0.0.0:9229`，项目代码本身尚未报错。
- 恢复方案：使用已允许的站点开发命令在受限环境外重新启动。
- 最终结果：开发服务已成功运行，本地地址为 `http://localhost:3000/`。
- 预览说明：当前会话没有提供 Sites 规范中提到的应用内打开工具，因此暂未自动打开页面；服务保持运行。

### 06. 检查初始化后的核心文件

- 操作：读取页面、布局、全局样式、依赖配置和 Sites 托管配置。
- 目的：确认现有项目结构与可复用依赖，再一次性实现首版交互式产品。
- 结果：确认项目为 Next.js/Vinext + React + TypeScript + Tailwind；当前页面仍是临时加载骨架，托管配置未启用数据库或对象存储。
- 决策：首版采用纯浏览器 Canvas 处理，图片不上传服务器；实现 JPG/PNG/WebP 输出、目标体积压缩、分辨率和模板裁剪。HEIC/AVIF 等受浏览器解码/编码能力限制的格式先做清晰提示，后续再接隔离服务端 Worker。
- 视觉：以已确认的浅色/深色移动端原型为准，并在桌面宽屏上增加居中工作台，而不是复制桌面管理后台。

### 07. 调整界面依赖

- 操作：移除仅供初始骨架使用的 `react-loading-skeleton`，加入统一线性图标库 `lucide-react`。
- 目的：删除不再需要的启动页依赖，并保证功能图标风格一致、可访问且易维护。
- 结果：`react-loading-skeleton` 已移除。首次安装 `lucide-react` 时受限网络无法解析 npm 地址，获得授权后重试成功。
- 依赖审计：npm 报告现有依赖树有 16 个漏洞（1 low、9 moderate、6 high）；本轮不执行可能引入破坏性升级的 `npm audit fix --force`，在功能完成后单独核查来源和可修复范围。

### 08. 实现首版产品

- 操作：替换临时骨架，编写移动端优先的完整单页工具、浅色/深色主题和浏览器端图片处理逻辑；同步更新站点元数据与全局设计变量。
- 目的：形成从上传到编辑、模板裁剪、处理结果和下载的完整可用闭环。
- 计划修改：`app/page.tsx`、`app/globals.css`、`app/layout.tsx`；移除不再引用的 `_sites-preview` 文件。
- 结果：已完成上传、格式转换、目标体积压缩、分辨率调整、模板裁剪、前后对比、结果下载、响应式导航和浅色/深色主题。
- 本地处理：使用浏览器 Canvas 完成处理；JPEG 输出自动铺白色背景，模板使用居中 cover 裁剪，JPEG/WebP 目标体积通过质量二分搜索逼近。
- 兼容提示：首版明确提示 30 MB 上限以及 HEIC/RAW 等浏览器无法解码格式的后续服务端支持计划。
- 清理：已删除临时骨架组件和样式，替换默认标题、描述与页面语言。
- 自检修正：避免结果 URL 更新时错误释放原图 URL；模板入口会先切换到裁剪工具再执行处理。

### 09. 检查代码变更并执行生产构建

- 操作：查看工作区变更摘要，然后运行完整生产构建。
- 目的：发现类型、编译和 Cloudflare/Vinext 输出兼容问题。
- 结果：生产构建成功，客户端、服务端、RSC、SSR 五个阶段均通过；共转换客户端 1813 个模块。
- 提示：Vinext 对根路由给出静态/动态分类尚不确定的非阻断说明，不影响构建产物生成。
- Git 状态：当前为新初始化项目，文件均尚未提交；未覆盖或丢失用户已有文档与原型。

### 10. 验证页面响应与自动测试

- 操作：请求正在运行的本地首页，并读取后执行项目内置页面测试。
- 目的：确认开发服务已加载新页面，而不只是构建器成功。
- 页面响应：生产渲染实际返回 200，并包含中文站点标题、首页上传入口、四项工具和隐私说明，证明新页面已进入构建产物。
- 测试结果：初始化器自带的两个测试仍专门断言“临时加载骨架必须存在”，因此在骨架按规范删除后均失败；失败内容与产品代码无关。
- 修复方案：将启动器测试改为正式产品验收测试，检查中文元数据、核心入口、隐私文案、无临时标记、主题变量和处理逻辑存在。

### 11. 更新正式产品测试

- 操作：替换 `tests/rendered-html.test.mjs` 中仅适用于启动骨架的断言。
- 目的：让自动测试验证当前产品，而不是已删除的临时界面。
- 结果：两个正式产品测试全部通过（2 passed，0 failed）。
- 覆盖范围：服务端中文页面、产品标题、四项核心工具、隐私说明、临时骨架清理、Canvas 处理逻辑、目标体积、模板、主题持久化、移动断点与减少动效偏好。

### 12. 生成站点社交分享预览图

- 操作：读取 `imagegen` 规范，并生成与“像素工坊”正式界面一致的横版社交卡片。
- 目的：改善链接在社交软件和消息应用中的预览效果。
- 约束：只生成一个正式方案；检查中文标题和核心文案后保存为 `public/og.png`，再接入站点元数据。
- 结果：使用内置图像生成模式产出 1 张正式横版分享卡片；已检查“像素工坊”“图片处理，一步完成”“转换·压缩·尺寸·裁剪”“本地处理，保护隐私”等主要文字可用。
- 文件：保存为 `public/og.png`，并接入 Open Graph 与 X/Twitter 大图元数据。
- 同步修正：目标体积算法现在尊重用户选择的最高画质，并在所有质量结果都高于目标时返回实际最小候选，避免错误保留更大的初始结果。

### 13. 最终构建与完整测试

- 操作：重新执行生产构建和正式产品测试，确保分享图、元数据和压缩算法修改均进入最终产物。
- 结果：生产构建再次成功；正式产品测试 2 项全部通过，0 失败。
- 当前状态：本地开发服务仍运行在 `http://localhost:3000/`，最终构建产物已生成。

### 14. 准备 Sites 托管与上线

- 操作：读取 `sites:sites-hosting` 技能说明，检查托管配置、源码状态与上线前置条件。
- 目的：按站点规范创建或关联托管项目并发布可访问版本。
- 结果：已创建 Sites 项目“像素工坊”，站点标识为 `pixel-workshop`；项目关联 ID 已写入 `.openai/hosting.json`。
- 安全：托管平台返回的短期源码写入凭证仅在发布命令中临时使用，不写入文件、远程地址或过程记录。

### 15. 固化并推送已验证源码

- 操作：检查 Git 分支与忽略规则，将当前已验证源码创建为首个提交，并使用短期凭证推送到 Sites 专用源码仓库。
- 目的：确保保存和部署的版本与刚才构建、测试通过的源码完全一致。
- 检查结果：项目位于 `main` 分支；依赖、构建产物和临时目录已按 `.gitignore` 排除，产品源码、架构文档、原型、测试和分享图均纳入版本。
- 提交结果：已创建首个源码提交并成功推送到 Sites 专用仓库的 `main` 分支。
- 凭证处理：源码推送使用一次性命令级授权，没有修改项目远程地址或写入凭证。

### 16. 打包、保存并发布站点版本

- 操作：将最终构建产物、托管配置和源码版本打包；保存一个 Sites 版本；优先部署为私有站点并轮询到终态。
- 目的：获得可访问的正式站点地址。
- 预检：正式源码已构建成功，正式测试 2/2 通过，源码已推送。
- 打包结果：官方打包脚本成功生成约 3.29 MB 的发布归档，共 41 个文件；源码提交与打包内容一致，工作区在打包时无未提交修改。
- 版本结果：Sites 已保存正式版本 v1。
- 部署结果：已按私有模式发布成功，正式地址为 `https://pixel-workshop.marcusyangty.chatgpt.site`。
- 预览说明：当前会话没有提供 `open_in_codex` 工具，因此无法自动打开正式站点；不影响发布成功。

### 17. 结束本轮开发服务

- 操作：在发布成功后停止本地开发服务，保留所有源码、构建配置、测试和过程记录。
- 目的：避免后台长期占用本机端口和资源。
- 结果：本地开发服务和隧道均已正常停止。
- 开发期说明：移除启动骨架依赖后，旧开发进程的热更新缓存曾短暂引用已删除组件并返回 500；进程随后完整重载，新页面恢复。之后两次生产构建和最终服务端渲染测试均通过，因此该热更新瞬态问题未进入发布版本。

### 18. 固化最终过程记录

- 操作：将发布结果、本地服务关闭结果和本轮摘要提交到本地 Git 历史。
- 目的：确保后续新对话或新开发者能够从本文档完整接续。
- 状态：执行后完成本轮。

## 本轮交付摘要

- 完成移动端优先、桌面自适应的正式网站界面；
- 完成浅色与深色模式及本地主题记忆；
- 完成浏览器端 JPG/PNG/WebP 转换、压缩、分辨率修改、模板裁剪、结果对比与下载；
- 完成目标体积二分搜索、透明图转 JPEG 白底、36 MP 输出保护和 30 MB 输入限制；
- 完成正式产品测试、生产构建、社交分享图和私有正式部署；
- 已知后续能力：图片增容的格式合法填充、HEIC/RAW/PSD/PDF 服务端 Worker、批量 ZIP、更多经核验的平台尺寸模板。

## 2026-07-17

### 19. 开始第二轮问题修复

- 用户反馈：模板需要直接支持抖音、小红书、微信公众号等热门社交媒体封面尺寸；图片压缩疑似不起作用。
- 操作：重新读取 `sites:sites-building` 规范，按现有站点 capability path 继续开发。
- 计划：检查压缩实现和格式分支，补充自动化用例；核验热门平台当前常用规格并扩展模板注册数据；重新构建、测试和发布。

### 20. 检查压缩与模板实现

- 操作：读取当前模板数据、文件载入默认格式、压缩编码分支和相关测试。
- 目的：先确认实际原因，再实施修复。
- 诊断结果 1：上传 PNG 时会默认继续输出 PNG，但浏览器 Canvas 的 PNG 编码忽略有损 `quality` 参数；当前压缩页虽然有提示，却没有提供格式切换入口，因此 PNG 用户会直接得到几乎未压缩的结果。
- 诊断结果 2：JPEG/WebP 当前只在 8%～用户画质上限之间搜索；如果原始分辨率过大，即使最低质量仍可能高于目标 KB，算法不会继续降低像素尺寸。
- 诊断结果 3：模板只有“视频封面、社交分享、方形头像”等通用名称，缺少用户可直接识别的平台入口。
- 修复方向：压缩页增加 JPG/WebP/PNG 选择；PNG 压缩默认推荐 WebP；编码质量不足以达标时按像素面积估算渐进缩放；结果页显示实际输出尺寸。

### 21. 核验热门平台模板规格

- 操作：检索抖音开放平台、微信相关规格资料和当前可核验的设计平台尺寸说明。
- 结果：抖音竖版内容采用 9:16 画布，主页封面常用 3:4 可见区域；小红书图文封面常用 3:4、视频常用 9:16；微信公众号首条封面采用约 2.35:1、次条小图采用 1:1；朋友圈封面可使用 9:16 长图并兼顾中间方形安全区。
- 数据策略：模板记录明确像素、比例、核验日期，并在界面提示平台可能二次裁剪，重点内容应放在安全区。
- 参考：抖音开放平台上传说明 `https://open.douyin.com/platform/resource/docs/openapi/video-management/douyin/create/upload/`；微信公众号尺寸说明 `https://www.canva.cn/sizes/wechat-official-account/`；朋友圈封面说明 `https://www.canva.cn/learn/wechat-circle-of-friends-cover-size/`。

### 22. 实施压缩修复与平台模板扩展

- 操作：修改 `app/page.tsx` 和 `app/globals.css`，同步补充产品测试。
- 压缩修复：压缩页新增 WebP/JPG/PNG 格式切换；PNG 输入进入压缩工具时默认使用 WebP；JPEG/WebP 通过 10 轮质量搜索逼近目标；最低质量仍超标时最多进行 7 次按面积估算的渐进缩放；PNG 也可通过缩放达到目标。
- 输出保护：如果目标体积大于原文件且同格式重编码反而变大，保留原文件；浏览器不支持请求的导出格式时给出明确错误；结果页展示最终实际分辨率。
- 模板扩展：加入抖音主页封面/竖版视频、小红书图文/视频封面、公众号首条/次条、朋友圈主页封面，以及通用视频、网页分享、头像、电商和证件照模板。
- UI：模板分类改为平台名称，加入安全区提示和用途说明；压缩页解释何时会降低分辨率，并对 PNG 给出醒目提示。
- 测试：加入压缩降维算法和三个热门平台模板的源码验收断言。

### 23. 执行构建与自动测试

- 操作：运行生产构建及正式产品测试。
- 结果：生产构建成功；正式产品测试 2/2 通过，0 失败。
- 构建说明：Vinext 仍给出根路由静态分类不确定的非阻断提示，与上一版本一致。

### 24. 执行真实浏览器流程检查

- 操作：读取 `browser:control-in-app-browser` 技能，启动本地开发服务，在页面中检查压缩和热门平台模板流程。
- 目的：验证用户可见行为，而不只依赖源码断言。
- 移动首页：在 390×844 视口中成功加载，品牌、主题切换、菜单、上传按钮和四项工具均出现在无障碍结构中。
- 阻碍：应用内浏览器无法自动向系统文件选择器写入文件；本机界面控制又因安全策略不能操作 Codex 应用，Chrome 扩展浏览器在当前会话不可用。
- 替代方案：加入仅在 `localhost?qa=compression` 生效的确定性测试入口，自动生成 1200×900 高熵 PNG，并调用与真实上传完全相同的 `handleFile` 与压缩处理流程；正式域名不会触发。
- 压缩结果：测试 PNG 原始体积 3.1 MB，压缩页自动选择 WebP；目标设为 200 KB 后，实际输出 197 KB、972×729，节省 94%，证明质量搜索与必要时降维均真实生效。
- 模板结果：抖音分类显示 1080×1440 和 1080×1920；小红书分类显示图文 1080×1440 和视频 1080×1920；微信分类显示公众号首条 900×383、次条 500×500、朋友圈 1080×1920。
- 裁剪结果：选择“公众号首条封面”并执行后，结果页实际输出 900×383。
- 额外发现：模板应用函数先更新输出格式再立即处理会读取旧 React 状态；当前没有造成尺寸错误，但可能让结果格式与待更新状态不一致。
- 修正：模板裁剪保持用户当前选择的输出格式，不再在同一事件中异步强制切换格式。
- 状态：真实流程验证通过。

### 25. 最终回归构建

- 操作：重新运行生产构建和自动测试，确认真实流程检查后的最后修正可发布。
- 结果：生产构建成功，正式产品测试 2/2 通过，0 失败。
- 浏览器收尾：已恢复默认视口并关闭本轮测试标签页。

### 26. 发布第二版站点

- 操作：读取 `sites:sites-hosting` 规范；提交并推送已验证源码，打包保存 Sites v2，部署到现有私有正式站点。
- 目的：让原线上地址获得压缩修复和热门平台模板。
- 源码：已提交并推送到现有 Sites 专用仓库；短期凭证未写入项目文件。
- 打包：官方脚本成功生成 v2 发布归档，打包时工作区无未提交修改。
- 发布：Sites v2 已保存并部署成功，继续使用正式地址 `https://pixel-workshop.marcusyangty.chatgpt.site`。
- 预览：当前会话没有提供 `open_in_codex` 工具，无法自动打开正式地址；发布状态已由 Sites 返回 succeeded。

### 27. 结束第二轮本地服务

- 操作：停止本地开发服务，保留第二轮验证结果和完整过程记录。
- 结果：本地开发服务和隧道均正常停止。
- 开发期提示：真实浏览器测试与热更新同时工作时，开发日志短暂出现 React 多渲染器 context provider 警告；页面请求仍为 200，压缩与裁剪流程成功，随后生产构建和正式测试均通过。该提示属于开发热更新环境，未影响 v2 发布构建。

### 28. 固化第二轮过程记录

- 操作：将发布结果、浏览器实测数据和服务关闭结果提交到本地 Git 历史。
- 状态：执行后完成第二轮。

## 第二轮交付摘要

- 已确认并修复 PNG 压缩看似无效的问题；
- 已通过 3.1 MB PNG → 197 KB WebP 的真实浏览器流程验证；
- 已支持抖音、小红书、微信公众号、朋友圈等平台模板；
- 已验证公众号首条封面实际裁剪输出为 900×383；
- 已完成移动端 390×844 检查、生产构建、自动测试和 v2 正式发布。

### 29. 开始第三轮功能开发

- 用户指令：继续开发后续能力。
- 操作：重新读取 `sites:sites-building` 规范，选择最初需求中仍未交付的“图片体积增大”作为本轮优先项。
- 产品决策：在现有“图片压缩”工作台内加入“增大体积”模式，避免新增第五个顶层入口挤压移动端导航；增容不改变可见画面，并明确说明不会提升真实画质。
- 技术方案：JPEG 使用合法 COM 注释段、PNG 使用合法 tEXt 辅助块、WebP 使用 RIFF JUNK 块补足目标体积；只允许增大到目标，不写入非法尾随垃圾字节。

### 30. 实现格式合法的文件增容核心

- 操作：新增独立的图片字节填充模块及单元测试，再接入现有处理流程和结果页。
- 核心模块：新增 `app/image-padding.ts`，分别实现 JPEG COM、PNG tEXt、WebP RIFF JUNK 增容，并更新 WebP RIFF 长度和 PNG CRC32。
- 边界：目标体积不大于当前文件时返回独立副本；JPEG/PNG/WebP 小于最小合法块的差值允许极小幅度向上取整；WebP 保持偶数字节对齐；最大增容目标限制为 60 MB。
- 格式细节：PNG tEXt 填充正文使用合法 Latin-1 空格字节，避免在文本内容中写入额外 NUL；关键字与正文之间只保留规范要求的单个 NUL 分隔符。
- 单元测试：JPEG、PNG、WebP 和无需增容四个用例全部通过（4/4）。
- 产品接入：将首页“压缩图片”升级为“文件体积”，编辑器提供“压缩体积 / 增大体积”切换；增容模式自动给出大于原文件的目标值，支持 WebP/JPG/PNG，并在结果页显示体积增加百分比。
- 用户说明：界面明确告知增容只增加格式合法的注释/填充数据，不添加噪点、不改变分辨率、不会提升真实画质。
- 测试命令：`npm test` 更新为构建后执行全部 `tests/*.test.mjs`。

### 31. 执行第三轮构建与完整测试

- 操作：运行 `npm test`，同时验证生产构建、服务端产品测试和图片增容单元测试。
- 首次结果：生产构建成功，6 项测试全部通过（JPEG、PNG、WebP、无需增容、服务端首页、产品基础），0 失败。
- 最后修正：进一步收紧 PNG tEXt 字节合法性并增加对应断言，准备执行最终回归。
- 最终结果：再次执行 `npm test`，生产构建成功，6/6 测试通过，0 失败。

### 32. 发布第三版站点

- 操作：读取 `sites:sites-hosting` 规范；提交并推送增容功能；打包、保存 Sites v3 并部署到现有私有正式站点。
- 源码：增容功能已提交并推送到现有 Sites 专用仓库；临时发布凭证没有写入仓库或 Git 配置。
- 打包：官方脚本成功生成 v3 发布归档，归档对应已验证并推送的同一源码版本。
- 发布：Sites v3 已保存并部署成功，正式地址仍为 `https://pixel-workshop.marcusyangty.chatgpt.site`。
- 预览：当前会话没有提供 `open_in_codex` 工具，未自动打开正式地址；Sites 部署状态已确认为 succeeded。
- 状态：完成。

### 33. 固化第三轮过程记录

- 操作：在正式发布成功后补充 v3 发布结果，并将最终过程记录提交到本地 Git 历史。
- 状态：完成。

## 第三轮交付摘要

- 已为 JPEG、PNG、WebP 增加格式合法的文件体积增大能力；
- 增容过程不改变可见画面、分辨率和真实画质；
- 已在移动端工作台加入“压缩体积 / 增大体积”模式切换；
- 已完成生产构建、6 项自动测试和 v3 正式发布。

### 34. 盘点当前开发进度与剩余范围

- 用户指令：检查当前开发进度，列出仍需开发的内容。
- 操作：对照最初需求、`PROJECT_ARCHITECTURE.md`、当前页面源码、测试、Git 状态和前三轮开发记录进行只读审计。
- 当前阶段：核心单图 MVP 已上线；JPG/PNG/WebP 的转换、压缩、合法增容、像素尺寸调整、热门平台模板裁剪、明暗主题和移动端主流程已经可用。
- 主要缺口：尚未达到“市面上大多数格式”，AVIF/HEIC/TIFF/GIF/SVG/BMP/ICO 及 RAW/PSD/PDF 仍缺服务端隔离处理；批量上传与 ZIP 下载未实现；模板裁剪仍为自动居中，缺少拖动主体、缩放、旋转和真实安全区控制；DPI、元数据清理、自定义模板、历史记录尚未实现。
- 获客缺口：当前只有单一首页，尚无独立 SEO 任务页、FAQ/结构化数据、站点地图、使用分析和错误指标，因此“快速上线”已经完成，“持续获得自然用户”的工程闭环尚未完成。
- 工程缺口：模板仍硬编码在页面，统一 `TransformSpec`、Preset Registry、服务端任务队列、格式 fixture/兼容矩阵、视觉回归和安全样本测试尚未落地；README 仍是脚手架说明；依赖审计记录的漏洞仍待专项处理。
- 下一步建议：优先完成批量处理与可调裁剪，再建设首批 SEO 落地页和数据分析；随后接入服务端专业格式转换，最后再做账户、历史、API 和商业化。
- 状态：完成。

### 35. 开始第四轮：批量处理与 ZIP 下载

- 用户指令：继续开发。
- 操作：按进度盘点的第一优先级启动“批量图片处理 + ZIP 下载”，重新读取 `sites:sites-building` 规范并检查现有单图处理结构。
- 本轮范围：支持一次选择或拖入多张图片，统一执行格式转换、目标体积压缩或按宽度等比缩放；逐张展示状态与结果；处理成功后在浏览器本地生成 ZIP 下载。
- 产品边界：首版批量上限 20 张、每张 30 MB；模板裁剪需要逐图调整主体，本轮不纳入批量，避免默认居中造成不可控裁切。
- 技术决策：继续保持图片不上传服务器；新增轻量 ZIP Store 编码模块并自行计算 CRC32，不引入额外运行时依赖。
- 状态：完成。

### 36. 实现批量处理工作台

- 操作：扩展文件选择和拖放入口，支持一次载入多张图片；新增批量队列、统一参数、逐张状态、失败信息、重新处理和 ZIP 下载界面。
- 支持范围：一次最多 20 张、每张最大 30 MB；批量支持统一转为 JPG/PNG/WebP、按目标 KB 压缩、按统一宽度等比缩放。
- 移动端：队列卡片在窄屏重排状态与删除按钮，控制面板改为单列并保留底部操作区；浅色和暗色继续使用同一套主题变量。
- 隐私：图片解码、转换、压缩、缩放和 ZIP 生成全部在浏览器本地执行。
- ZIP 核心：新增 `app/zip.ts`，实现 UTF-8 文件名、路径字符清理、CRC32、Local Header、Central Directory 和 EOCD，采用 Store 模式避免对已压缩图片重复压缩。
- 状态：完成。

### 37. 验证第四轮实现

- ZIP 单测：首次测试发现 EOCD 文件数量断言偏移写错，修正测试读取位置后 CRC32 标准向量和双文件 ZIP 结构 2/2 通过；实现本身的目录结构未改动。
- 完整验证：生产构建成功，包含图片增容、服务端产品、ZIP 在内的 8/8 自动测试通过，0 失败。
- 代码检查：ESLint 0 错误、5 个 `<img>` 性能提示；这些图片均为用户本地 `blob:` 预览，不适合交给远程图片优化器，保留原生元素。
- 类型检查：修复 ZIP 模块的 `Uint8Array<ArrayBufferLike>` 类型问题；全项目剩余 3 个错误均来自脚手架预置 Cloudflare Worker 全局类型未声明，与本轮代码无关，正式 Vinext 构建不受影响。
- 最终回归：类型修正后再次执行生产构建和全部测试，8/8 通过，0 失败；删除类型检查生成的临时 `tsconfig.tsbuildinfo` 缓存。
- 状态：完成。

### 38. 发布第四版站点

- 操作：准备提交并推送已经验证的批量处理源码，使用官方脚本打包 Sites v4，部署到现有私有正式站点。
- 源码：批量处理功能已提交并推送到现有 Sites 专用仓库，临时发布凭证没有写入项目。
- 打包：官方脚本成功生成 v4 归档，源码版本、构建产物和发布版本保持一致。
- 发布：Sites v4 部署成功，正式地址继续使用 `https://pixel-workshop.marcusyangty.chatgpt.site`。
- 预览：当前会话没有提供 `open_in_codex` 工具，未自动打开正式站点；Sites 状态已经确认为 succeeded。
- 状态：完成。

### 39. 固化第四轮记录

- 操作：补充正式发布结果和本轮交付摘要，并提交最终过程记录。
- 状态：完成。

## 第四轮交付摘要

- 已支持一次选择或拖入最多 20 张图片；
- 已支持批量转换格式、按目标 KB 压缩、按统一宽度等比缩放；
- 已提供逐张处理状态、失败信息、删除和重新处理；
- 已支持浏览器本地生成 UTF-8 文件名 ZIP 并一次下载全部成功结果；
- 已完成生产构建、ESLint 检查、8 项自动测试和 v4 正式发布。

### 40. 评估社交媒体分享链接导入功能

- 用户需求：粘贴小红书、Instagram、X、抖音等平台分享链接，解析帖子内全部图片和视频并让用户选择下载，期望获得无水印媒体。
- 操作：重新读取 `sites:sites-building` 规范，并核验 Meta/Instagram、X、抖音和小红书当前公开的官方开发能力与授权限制。
- 核验结论：这些平台没有一个统一、无需授权且允许对任意分享链接下载全部原始媒体的官方接口。X API 需要开发者方案和 Bearer Token；抖音视频查询能力需要申请权限和用户授权，且官方接口限制为授权用户相关视频；Instagram 的媒体访问以授权账户/专业账户能力为主，公开内容嵌入不等于提供全部原始媒体下载；小红书公开开放能力主要面向小程序、商家和服务商授权，没有发现任意公开笔记媒体下载接口。
- 合规边界：可以开发“用户本人或已获授权内容的媒体导入与下载”；不会实现绕过登录、反爬、访问控制或对画面中已嵌入水印进行擦除。若平台官方返回的源文件本身不带水印，可原样提供下载。
- 架构影响：该功能需要新增服务端 Provider Adapter、OAuth/密钥管理、链接白名单与 SSRF 防护、媒体下载代理、限流、临时缓存和版权确认；不能继续使用当前完全浏览器本地处理模式完成真实解析。
- 阻塞项：需由用户确认是否接受“官方授权、仅本人或获授权内容”的产品范围；确认后再选择首批平台并申请对应开发者凭证。
- 状态：等待用户确认范围。

### 41. 调研同类本地项目并形成技术方案

- 用户补充：只接受本地处理，要求先调研市面同类项目再确定方案。
- 操作：调研 cobalt、yt-dlp、XHS-Downloader、DouK-Downloader、TikTokDownload/F2，以及浏览器 CORS、Chrome Native Messaging、Tauri sidecar 和本地网络访问约束。
- 关键发现：同类项目能够实现多平台媒体提取，但可靠实现均需要本地可执行程序、本地 API、Docker 或浏览器扩展；普通网页无法稳定跨域读取平台页面、Cookie 和签名接口。
- 推荐方案：保留现有在线 UI，新增 `Pixel Workshop Local Companion`；本地程序监听 loopback，使用 yt-dlp 处理 Instagram/X/TikTok，并为小红书、抖音建立独立 Adapter；媒体与 Cookie 不经过项目服务器。
- 许可证发现：cobalt 为 AGPL-3.0，XHS-Downloader 和 DouK-Downloader 为 GPL-3.0，TikTokDownload/F2 为 MIT；正式复用前需按许可证选择隔离调用、独立实现或保留声明。
- 文档：新增 `SOCIAL_MEDIA_LOCAL_IMPORT_RESEARCH.md`，记录竞品能力、三种本地方案、推荐架构、安全通信、产品流程和分阶段实施路线。
- 状态：调研与方案完成，等待用户确认是否启动本地 Companion 原型。

### 42. 整理仓库并编写服务器部署文档

- 用户指令：暂缓社交媒体链接解析开发；将调研保留在文档；编写可直接跟随操作的完整服务器部署文档，整理后推送到 `TheYangTy/picture_tool`。
- GitHub 检查：本地仓库尚未配置 remote；GitHub CLI 已安装，但 `TheYangTy` 的现有认证 token 失效，推送前需要重新授权。
- 运行验证：实际执行生产构建后的 `vinext start --hostname 127.0.0.1 --port 3180`，服务成功启动；访问首页返回 HTTP 200 并包含正式产品内容。
- 仓库整理：将 npm 包名从脚手架名称更新为 `pixel-workshop`；重写产品 README；新增健康检查、Dockerfile、Docker Compose、systemd、Nginx 和 GitHub Actions CI 配置。
- 部署文档：新增 `DEPLOYMENT.md`，覆盖 Ubuntu/Node/systemd/Nginx/HTTPS、防火墙、Docker、健康检查、日志、更新、回滚、备份、排障和上线检查清单。
- 安全默认值：Node/容器端口只绑定本地回环地址；容器只读、删除 capabilities、禁止提权；systemd 使用独立非 root 用户；Nginx 提供基础安全响应头。
- 测试：新增 `/api/health` 产品测试和部署文件验收断言；生产构建成功，9/9 自动测试通过，ESLint 0 error（保留 5 个本地 `blob:` 图片使用原生 `<img>` 的提示）。
- 容器验证：`docker compose config` 通过；生产镜像构建成功；以只读文件系统、无 capabilities、禁止提权配置启动临时容器，健康接口返回 200，Docker 状态为 healthy，随后停止临时容器。
- 依赖审计：生产依赖没有 high/critical，存在 2 个 Next.js 间接 PostCSS moderate 提示；npm 仅给出可能导致破坏性版本变化的 `--force` 方案，本轮记录风险而不执行强制修复。
- GitHub 目标：只读检查确认 `TheYangTy/picture_tool` 仓库存在且当前为空；已将其设置为本地 `origin`。由于空仓库没有 base branch，本次应作为首次提交直接推送 `main`，不创建无意义的 Draft PR。
- 状态：文档、配置与验证完成，等待恢复 GitHub 登录后推送。

### 43. 首次推送到 GitHub

- 操作：将 `https://github.com/TheYangTy/picture_tool.git` 设置为 `origin`；提交部署文档、生产配置、健康检查、CI 和 README 整理结果；首次推送完整项目历史到远程 `main`。
- 认证结果：虽然 `gh auth status` 显示 GitHub CLI 的旧 token 已失效，但系统 Git 凭据仍可正常完成用户明确授权的 HTTPS push，因此无需阻塞发布或要求重新登录 CLI。
- 远程验证：`origin/main` 创建成功；远程 main 与本地 HEAD 指向相同提交；工作区无未提交代码改动。
- PR 决策：目标仓库推送前为空，没有可作为 base 的默认分支；本次按初始化仓库场景直接建立 `main`，未创建 Draft PR。
- 状态：项目已成功推送到 `TheYangTy/picture_tool`，准备补推本条最终过程记录。

### 44. 启动 Apple Design 移动端 UI 重设计

- 用户指令：学习 `emilkowalski/skills` 中的 Apple Design 方法，为项目设计一版新的移动端 UI 原型图。
- 操作：完整阅读用户指定的 `apple-design/SKILL.md`，并重新读取本项目适用的 `sites:sites-building` 网站设计规范；本轮仅制作独立原型，不改动线上版本。
- 方法提炼：以目的、掌控感、熟悉度、灵活性、简单性、工艺和愉悦为设计基础；交互层采用按下即反馈、内容与手势 1:1、可中断动效、空间来源一致、渐进阻尼和减少动态效果适配；视觉层采用系统字体、按字号调整字距和行高、克制的半透明功能层以及明暗主题一致的层级。
- 产品映射：把原有“多工具平铺”调整为“先选择图片，再按当前任务逐步展开”；上传页只保留一个明确主操作，编辑页让压缩、格式、尺寸、裁剪控件紧邻图片结果，并持续显示预计体积和节省比例。
- 原型范围：一张 1600 × 1000 的双屏移动端方案，左侧展示浅色导入态，右侧展示深色编辑态；同时覆盖热门社交媒体尺寸入口、本地隐私说明、压缩预估和导出动作。
- 状态：设计稿 HTML 已创建，等待渲染和视觉验收。

### 45. 完成 Apple Design 移动端原型与视觉验收

- 原型文件：新增 `prototypes/apple-design-mobile-v3.html`，采用纯 HTML/CSS 构建 1600 × 1000 双屏概念稿，未引入外部字体、图片或运行时依赖。
- 首次渲染：先使用 Sites 自带的 macOS HTML 预览脚本导出静态图；确认系统缩略图的居中裁切导致右侧编辑屏展示不完整，因此未将该结果作为最终稿。
- 修正导出：通过本机只读 HTTP 预览和 1600 × 1000 浏览器视口重新截图，完整保留浅色导入态与深色编辑态；最终图片保存为 `prototypes/apple-design-mobile-v3.png`。
- 视觉验收：检查大标题字距与行高、单一主操作层级、上传卡与社交媒体尺寸入口、深色预览区、工具分段、压缩滑杆、预计结果和底部导出按钮，双屏均无裁切或横向溢出。
- 可访问性：原型采用系统字体、足够的明暗对比度、44px 左右触控目标、按下缩放反馈，并提供 `prefers-reduced-motion` 与 `prefers-reduced-transparency` 降级样式。
- 线上影响：本轮没有修改现有应用页面，也没有发布或推送；待用户确认视觉方向后再进入真实组件与交互实现。
- 状态：完成。

### 46. 启动 Apple Design 交互原型独立部署

- 用户指令：将可交互原型部署成站点，以便直接查看效果。
- 发布边界：创建独立的原型预览站点，不覆盖现有“像素工坊”正式工具站；原型站只用于确认新版移动端视觉与交互方向。
- 本轮目标：把静态双屏概念稿重构为真实响应式单屏流程，支持浅色/深色切换、选择图片演示、编辑工具切换、压缩滑杆实时反馈、常用平台模板选择、返回与重置。
- 操作规范：使用 `sites:sites-building` 完成构建和验证，再使用 `sites:sites-hosting` 发布；部署结果与访问地址继续记录在本文档中。
- 实现：新增独立 Sites 原型项目 `prototypes/apple-design-live`；将双屏静态概念重构为真实响应式单屏流程，移动端使用全屏布局，桌面端以居中的移动应用画布展示。
- 交互：支持系统主题初始化与手动明暗切换、本地图片选择和预览、示例图片体验、压缩/格式/尺寸/裁剪工具切换、压缩滑杆与预计体积联动、JPG/PNG/WebP 选择、平台封面模板切换、重置与返回。
- 隐私边界：文件通过浏览器 `blob:` URL 本地预览，不上传服务器；导出按钮明确标记为原型演示，不伪装为已实现的真实处理结果。
- 分享预览：使用 imagegen 生成专属 `public/og.png`，画面复用浅色导入态、深色编辑态和蓝色品牌色；检查确认“像素工坊”“图片处理，清爽一点。”文字可用，并接入随请求域名生成的 Open Graph/X 元数据。
- 工程整理：移除 Sites 临时骨架与 `react-loading-skeleton`，更新项目名称、README、产品元数据与针对原型的服务端渲染测试。
- 验证：最终生产构建成功；2/2 自动测试通过；本地页面返回正式原型标题与内容。
- 发布：以独立私有 Sites 项目发布第 1 版，地址为 `https://pixel-workshop-ui.marcusyangty.chatgpt.site`；原“像素工坊”正式工具站未被覆盖。
- 访问检查：未登录的公网请求返回 HTTP 401，符合私有站点预期；项目拥有者在已登录的 ChatGPT/Codex 环境中可访问。
- 工具说明：当前会话未提供 Sites 规范中用于自动打开部署地址的 `open_in_codex` 能力，因此不影响发布成功，改为在最终交付中直接提供访问链接。
- 状态：完成。

### 47. 精简原型首屏文案

- 用户反馈：首屏“一切都在本地完成”“图片处理，清爽一点。”表达过多，并要求删除“UI PROTOTYPE”。
- 文案调整：删除功能区上方的小标题；主标题改为单行“轻松处理图片”；辅助说明精简为“选择图片，马上开始。”。
- 界面调整：移除导航栏中央的“UI PROTOTYPE”标识及对应样式，保留品牌名称、主题切换和上传区域下方的本地隐私说明。
- 分享预览：使用内置 imagegen 重新生成只包含“像素工坊”品牌名的简洁封面，保存为 `public/og-v2.png` 并替换链接预览引用；旧封面文件保留以避免破坏历史版本。
- 测试修正：首次验证发现旧测试仍要求页面出现已删除的“UI PROTOTYPE”；将断言改为明确验证该标识和两段旧文案均不存在。
- 发布计划：更新同一个独立原型站，不影响正式工具站。
- 验证：生产构建成功，2/2 自动测试通过；测试同时确认“UI PROTOTYPE”“一切都在本地完成”“图片处理，清爽一点。”均未出现在服务端渲染结果中。
- 发布：原型站第 2 版已成功部署到原地址 `https://pixel-workshop-ui.marcusyangty.chatgpt.site`。
- 状态：完成。

### 48. 启动主项目 UI 原型落地

- 用户指令：按照已确认的 Apple Design 原型更新正式项目 UI，并完成自测与全部测试。
- 改造边界：保留当前格式转换、真实压缩、合法增容、分辨率调整、热门平台模板裁剪、批量处理与 ZIP 下载实现，只调整页面结构、视觉层级和交互呈现。
- 首屏结构：移除桌面导航和移动菜单，保留品牌与主题切换；采用“轻松处理图片 / 选择图片，马上开始。”的单任务入口；上传区改用原型中的双图片视觉；新增小红书、抖音、公众号常用尺寸快捷入口以及轻量工具栏。
- 隐私呈现：保留“图片不会上传，关闭页面后不留痕迹”，并继续允许单张或最多 20 张图片本地处理。
- 编辑工作台：单图编辑改为居中的纵向任务流，文件信息、深色图片预览、四工具分段控件、参数面板和底部主操作保持同一视觉轴线；桌面端最大宽度收敛，移动端使用更紧凑的单手布局。
- 主题系统：正式页面变量更新为原型的近白/近黑背景、系统蓝主色、克制半透明层和更柔和阴影；加入减少透明度适配，同时保留已有减少动态效果适配。
- 测试更新：服务端渲染测试改为验证新标题、新上传入口、三类热门平台快捷尺寸、隐私文案和关键原型样式，并反向检查旧首屏营销文案不再出现。
- 首轮验证：生产构建和 9/9 自动测试通过；根项目 ESLint 首次失败是因为扫描了此前独立部署、拥有自己构建产物和配置的 `prototypes/apple-design-live` 子项目，而非主项目本次 UI 代码错误。
- 检查边界修正：将独立原型项目加入根 ESLint 全局忽略列表；该原型已经在自身项目内完成独立测试，主项目检查现在只覆盖主项目源码。
- 代码质量：根项目 ESLint 最终为 0 error；保留 5 条本地 `blob:` 图片使用原生 `<img>` 的框架建议，不影响浏览器本地预览、图片对比或生产构建。
- 移动端浏览器自测：在 390 × 844 视口验证新版首页、常用平台入口、四类图片工具和本地隐私提示，页面无横向溢出；浅色首页与深色结果页截图保存为 `prototypes/main-ui-mobile-home-v4.png` 和 `prototypes/main-ui-mobile-result-dark-v4.png`。
- 主题验证：从浅色模式切换到深色模式后，根节点主题状态正确变为 `dark`，结果页按钮、卡片、对比预览和安全提示保持清晰层级。
- 真实压缩回归：通过仅在 localhost 生效的确定性 QA 图片进入“文件体积”工作台；将 1200 × 900、3.1 MB 的 PNG 压缩为 972 × 729、197 KB 的 WebP，页面显示节省空间 94%，下载和继续调整入口正常出现。
- 浏览器稳定性：完整压缩流程结束后检查浏览器错误日志，结果为空；测试标签页和临时移动端视口已清理。
- 最终复验：`git diff --check` 通过；生产构建再次成功；9/9 自动测试再次通过；ESLint 保持 0 error、5 条预期提示。
- 发布计划：复用 `.openai/hosting.json` 中已有正式站点项目，只发布本次主项目 UI 源码，不修改站点访问权限，也不将独立原型子项目加入本次源码提交。
- 源码提交：本次主项目 UI、测试与两张移动端验收截图提交为 `b355f46`；独立原型目录保持在本地且未混入该提交。
- Sites 发布：将 `b355f46f5fc77aa4131fbbf8a1833a581aa4b495` 推送到项目绑定的 Sites 源仓库，打包同一提交生成的 `dist`，保存为正式站点第 5 版并完成私有生产部署。
- 发布结果：部署状态为 `succeeded`，正式地址继续使用 `https://pixel-workshop.marcusyangty.chatgpt.site`；站点访问权限未修改。
- 状态：完成。

### 49. 扩展图片预设模板库

- 用户反馈：当前图片预设数量偏少，希望参考移动端图片工具的预设面板增加更多模板。
- 规范核对：检查 Instagram、X 与 YouTube 的官方帮助说明；Instagram 当前支持宽度最高 1080px、比例范围 1.91:1 至 3:4，X 官方推荐头像 400×400、横幅 1500×500，YouTube 当前推荐缩略图 3840×2160 且使用 16:9，播客播放列表使用 1:1。
- 模板扩展：新增常用比例 1:1、4:3、16:9、3:4、9:16，以及 Instagram 帖子/Story、X 头像/横幅/配图、YouTube 视频/播客、哔哩哔哩视频、视频号等预设；保留现有抖音、小红书、公众号、电商、证件照与通用模板。
- 交互调整：模板分类改为横向可滚动的 11 类标签；默认展示“常用比例”；搜索范围扩展到平台、名称、比例和说明；增加当前筛选数量、模板总数与无结果状态。
- 稳定性修正：首页三个快捷尺寸改为按模板 ID 获取，不再依赖数组下标，后续继续增加或调整模板顺序不会错误指向其他平台。
- 测试计划：补充新增平台、常用比例、分类和空状态的源码断言，随后执行生产构建、完整自动测试与移动端浏览器回归。
- 浏览器测试准备：尝试使用用户附图直接进入本地模板页时，浏览器安全边界拒绝了自动文件注入；未上传图片、未改变原文件。为保证测试可重复，扩展仅在 localhost 生效的确定性 QA 入口，使用浏览器 Canvas 生成测试图并直接进入模板页，生产域名不会执行该逻辑。
- 首轮验证：生产构建成功，9/9 自动测试通过；ESLint 0 error，保留 5 条本地 `blob:` 图片使用原生 `<img>` 的预期提示。
- 移动端回归：在 390×844 视口打开模板页，默认正确显示 5 个常用比例与“共 28 个”总数；分类栏完整包含 11 类且页面没有横向溢出。
- 分类与裁剪验证：Instagram 分类正确显示 4 个模板；选择“Instagram 竖版帖子”后实际输出 1080×1440 WebP，尺寸与模板一致。
- 搜索验证：切换“全部”后搜索“X 主页”只返回 1500×500 的 X 主页横幅；搜索不存在内容时显示明确空状态。
- 浏览器稳定性：模板分类、搜索、空状态和裁剪完整流程无控制台错误；测试标签页与临时移动端视口已清理。
- 最终复验：`git diff --check` 通过；生产构建再次成功；9/9 自动测试再次通过；ESLint 保持 0 error、5 条预期提示。
- 发布状态：尝试沿用项目现有 Sites 发布流程时，运行环境要求用户再次明确确认“向远程主分支推送并发布”；本轮未绕过该保护，也未执行远程推送或线上更新。
- 用户授权：用户已明确回复“统一推送并发布到正式站点”，授权将本轮模板库更新推送到 GitHub 与项目绑定的 Sites 源仓库，并部署到现有正式站点；保持当前访问权限不变。
- 统一推送：模板库代码与授权记录已推送到 GitHub `main` 和项目绑定的 Sites 源仓库 `main`，两端使用相同源码状态。
- 正式发布：同一源码状态保存为正式站点第 6 版，私有生产部署状态为 `succeeded`，访问地址为 `https://pixel-workshop.marcusyangty.chatgpt.site`，访问权限未修改。
- 工具说明：当前会话未提供 Sites 规范中的自动打开部署地址能力，不影响发布结果，最终直接提供正式访问链接。
- 状态：完成。

### 50. 调整首页功能层级并增加自由尺寸

- 用户确认：同意将四个核心图片工具与尺寸快捷区交换位置，并要求结合整体审美调整“热门平台尺寸”表达，同时在尺寸修改中加入自由尺寸能力。
- 信息层级：首页上传区之后先展示“图片工具”，再展示“热门平台尺寸”；核心功能从轻量文字条升级为四张紧凑任务卡，包含彩色功能图标、功能名称、说明和进入箭头，移动端保持 2×2 排列。
- 文案调整：“常用尺寸”改为“热门平台尺寸”，辅助说明改为“一键进入模板裁剪”，明确它是平台裁剪快捷入口，避免与“调整尺寸”功能混淆。
- 自由尺寸：单图“调整尺寸”新增“自由尺寸 / 按比例缩放”分段选择；自由尺寸允许宽、高独立输入 1–12000px，并可按需锁定原图宽高比。
- 比例缩放：提供 25%、50%、75%、100%、150%、200% 六档，实时展示每档输出像素和当前输出尺寸；切换模式时恢复原图比例，减少误操作。
- 测试更新：增加首页模块顺序、热门平台文案、自由宽高输入、两种尺寸模式和新增视觉组件的断言。
- 验证：`git diff --check` 通过；生产构建成功；9/9 自动测试通过；ESLint 0 error，保留 5 条本地 `blob:` 图片使用原生 `<img>` 的预期提示。
- 发布范围：仅更新主项目首页功能层级、工具卡视觉和单图尺寸编辑，不修改图片处理算法、平台模板库或站点访问权限。
- 统一推送：本轮首页层级与自由尺寸更新已推送到 GitHub `main` 和项目绑定的 Sites 源仓库 `main`。
- 正式发布：相同源码保存为正式站点第 7 版并完成私有生产部署，状态为 `succeeded`，地址继续使用 `https://pixel-workshop.marcusyangty.chatgpt.site`，访问权限未修改。
- 状态：完成。

### 51. 修复模板裁剪无法移动与缩放

- 用户反馈：模板裁剪预览中的虚线框无法操作，图片不能在框内放大、缩小或调整位置。
- 原因确认：此前虚线框仅为静态安全区提示，预览使用固定 `object-fit: cover`，导出算法也始终执行居中 cover 裁剪；“重置位置”按钮没有绑定实际行为。
- 交互方案：保持输出画布和虚线安全区固定，让用户直接拖动底层图片调整主体位置；新增 100%–400% 缩放滑杆、放大/缩小按钮、鼠标滚轮缩放、移动端双指缩放与一键复位。
- 导出一致性：新增纯函数 `getCropSourceRect`，根据原图、目标比例、缩放倍数与水平/垂直焦点计算实际源图裁剪矩形；预览和导出共享同一组位置与缩放状态，避免只改变界面、不改变结果。
- 边界控制：拖动位置限制在 0%–100%，缩放限制在 1–4 倍；图片始终覆盖输出框，不允许拖出空白区域；切换图片或模板时自动回到居中 100%。
- 可用性：裁剪区增加抓取光标、触摸手势禁用页面误滚动、明确的“拖动图片调整位置”提示，以及可键盘操作的缩放 range 控件。
- 测试：新增居中裁剪、左右边缘定位、指定焦点缩放和越界保护四项纯函数测试，并扩展页面交互与样式断言。
- 自动验证：生产构建成功，13/13 自动测试通过（新增 4 项裁剪数学测试）；ESLint 0 error，保留 5 条本地 `blob:` 图片使用原生 `<img>` 的预期提示。
- 移动端交互回归：在 390×844 视口通过 localhost 确定性测试图进入裁剪器；放大按钮将缩放从 100% 正确更新到 110%，预览图片尺寸随之变化。
- 拖动验证：在 110% 缩放下拖动图片后，预览定位值由 `left -23.33% / top -5%` 更新为 `left -4.22% / top 0%`，证明水平与垂直位置真实改变且边界未露出空白。
- 复位验证：点击“复位”后缩放回到 100%，图片恢复居中定位；再次放大和反向拖动后执行裁剪，成功输出 1080×1080 WebP，说明交互状态已进入真实导出流程。
- 浏览器稳定性：放大、拖动、复位、再次拖动及导出完整流程无控制台错误；测试标签页和临时移动端视口已清理。
- 统一推送：交互裁剪修复、裁剪数学模块与新增测试已推送到 GitHub `main` 和项目绑定的 Sites 源仓库 `main`。
- 正式发布：相同源码保存为正式站点第 8 版并完成私有生产部署，状态为 `succeeded`，地址继续使用 `https://pixel-workshop.marcusyangty.chatgpt.site`，访问权限未修改。
- 状态：完成。

### 52. 部署到腾讯云并启用公网子路径

- 用户指令：将项目直接部署到用户的腾讯云服务器，开放公网访问，并挂载到 `https://yangtianyu.cloud/pictool` 路径。
- 部署边界：本轮目标是用户指定的腾讯云 Linux 服务器，不更新或替换现有 Sites 站点；继续保持图片全部在浏览器本地处理，服务器仅提供静态资源、前端运行文件与健康检查。
- 初步检查：项目已有 Docker Compose、systemd、Nginx、健康检查和完整服务器部署文档；现有 Nginx 模板面向独立域名根路径，需要为 `/pictool/` 子路径补充反向代理与前缀处理，并验证前端资源在子路径下可正常加载。
- 安全原则：应用进程只监听服务器回环地址，公网仅开放 Nginx 的 80/443；复用域名现有 HTTPS 配置，部署前检查服务器已有站点配置，避免覆盖同域名其他路由。
- 连接探测：本机 SSH 配置记录服务器 `43.134.53.23`、用户 `root`；首次使用 SSH 默认密钥进行只读检查时服务器拒绝公钥认证，未进入服务器、未产生任何远端改动。下一步尝试本机专用于云服务器的 `codex_cloud` 密钥，并继续保持只读检查。
- 连接恢复：使用本机已有的云服务器专用密钥成功只读登录；服务器为 Debian 12，已安装 Node.js 22.22、npm 10.9、Nginx 1.22、Docker 28.4，内存约 1.8GiB、可用磁盘约 6.8GiB。
- 域名与证书：服务器侧确认 `yangtianyu.cloud` 解析到 `43.134.53.23`；Let's Encrypt 证书覆盖根域名与 `www`，有效期至 2026-10-10；当前根站 HTTPS 返回 200。
- 路由现状：现有 Nginx 已预留 `/pictool` 并代理到 `127.0.0.1:3000`，但该端口没有服务，所以公网当前返回 502；主站使用 3001，其他 dashboard 与 `/assets/` 路由也已占用，不能让图片工具继续输出根 `/assets/` 地址。
- 服务器隔离：服务器尚无 `pixelworkshop` 系统用户、`/opt/pixel-workshop` 目录或 systemd 服务；3000 端口空闲，Nginx 当前配置检查通过。
- 子路径适配：Vite 构建新增可选 `PUBLIC_BASE_PATH`，腾讯云部署时设置为 `/pictool/`，其他根域部署保持 `/`；新增专用 Nginx location 片段，统一把 `/pictool/` 页面、资源和健康检查去前缀转发到回环 3000 端口，避免与主站资源冲突。
- 本地验证：使用 `PUBLIC_BASE_PATH=/pictool/` 完成生产构建，13/13 自动测试通过，ESLint 0 error；生产首页生成的 CSS/JavaScript 地址均以 `/pictool/assets/` 开头。
- 代理验证：临时启动生产服务后，根页面与 `/api/health` 返回 200；模拟 Nginx 去掉 `/pictool` 前缀后，对应 CSS 和 JavaScript 资源均返回 200 且带长期不可变缓存头，临时服务随后停止。
- 状态：子路径构建已验证，准备提交源码并在腾讯云创建独立用户、release 与服务。
- 源码发布：子路径构建配置、Nginx 示例和对应测试提交为 `bece971`，并已推送到 GitHub `main`，确保服务器部署来源可追溯。
- 远端准备：创建无登录权限的 `pixelworkshop` 系统用户和 `/opt/pixel-workshop/releases` 发布目录，将 `bece971` 克隆为独立 release；首次使用 root 身份读取该仓库状态时触发 Git 所有权保护，仅影响状态检查，未修改全局 Git 安全配置，后续统一以应用用户执行仓库与构建操作。
- 服务器验证：以 `pixelworkshop` 用户安装锁定依赖并使用 `PUBLIC_BASE_PATH=/pictool/` 构建；13/13 自动测试全部通过，ESLint 0 error，保留 5 条本地 Blob 图片预览使用原生 `<img>` 的预期性能提示。
- 服务启动：将 `bece971` 指向 `/opt/pixel-workshop/current`，安装并启用 `pixel-workshop.service`；服务状态为 active，`/api/health` 返回正常，监听地址确认仅为 `127.0.0.1:3000`。
- Nginx 切换：下载现有站点配置并只替换原失效的 `/pictool` 区块；服务器保留 `backup-bece971` 备份，`nginx -t` 通过后完成平滑重载，未覆盖主站或其他 dashboard 路由。
- 首次公网检查：`/pictool` 正确 301 到 `/pictool/`，页面、CSS、JavaScript 和 `/pictool/api/health` 均返回 200，根站仍返回 200；进一步检查发现框架字体预加载、favicon 和分享图仍使用根路径，可能与主站资源命名空间冲突。
- 子路径完善：移除不受 Vite base 控制的 `next/font` 运行时字体资源，改用 Apple 系统字体栈；favicon 与 Open Graph/Twitter 分享图根据 `PUBLIC_BASE_PATH` 生成路径，并增加源码测试防止回归。
- 修正验证：再次以 `/pictool/` 前缀完成生产构建，13/13 自动测试通过，ESLint 0 error，`git diff --check` 通过；准备发布修正 release 并执行最终公网验收。
- 运行时复查：修正 release `70993f5` 在服务器完成依赖安装、13/13 测试与 lint 后切换上线；CSS/JavaScript、favicon 和分享图接口本身均返回 200，但 HTML 元数据仍显示根路径。确认原因是元数据由运行中的服务生成，而 systemd 未传入构建时使用的 `PUBLIC_BASE_PATH`。
- 服务配置修正：在 systemd unit 中固定 `PUBLIC_BASE_PATH=/pictool/`，并增加自动测试约束，确保构建期资源与运行时元数据使用同一公网前缀。
- 依赖审计：完整依赖审计报告包含开发工具链问题；进一步执行生产依赖审计后仅剩 Next.js 内部 PostCSS 的 2 项 moderate，官方自动修复建议会强制降级到不兼容的 Next.js 9，因此未执行破坏性 `npm audit fix --force`。当前生产依赖没有 high 或 critical 报告。
- 最终 release：systemd 前缀修正提交为 `66dc085` 并推送到 GitHub `main`；服务器当前 release、Git HEAD 与 systemd unit 均对齐该提交，服务环境包含 `NODE_ENV=production`、`PORT=3000` 和 `PUBLIC_BASE_PATH=/pictool/`。
- 最终公网验收：`https://yangtianyu.cloud/pictool/`、CSS、JavaScript、favicon、Open Graph 分享图和 `/pictool/api/health` 全部返回 200；HTML 不再包含冲突的根 `/assets/`、`/favicon.svg` 或 `/og.png` 引用，`/pictool` 无尾斜杠地址正确 301 跳转。
- 隔离与回归：应用端口继续只监听 `127.0.0.1:3000`，公网由现有 HTTPS Nginx 提供；`https://yangtianyu.cloud/` 原主站保持 200，未改动其应用服务、证书和其他路由。服务器保留旧 release 与 Nginx 备份用于回滚。
- 状态：腾讯云正式部署完成，公网地址为 `https://yangtianyu.cloud/pictool/`。
